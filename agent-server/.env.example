PORT=4000
AUTH_TOKEN=your-secret-token-here
# API keys are passed from management server during provisioning - not stored here

# MCP endpoint URL for OCMT tools (vault, integrations, org resources)
# Defaults to https://YOUR_DOMAIN/api/mcp if not set
OCMT_MCP_URL=https://YOUR_DOMAIN/api/mcp
