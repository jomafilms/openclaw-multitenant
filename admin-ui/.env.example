# OCMT Admin UI Configuration

# Port to listen on
PORT=3000

# SECURITY: Bind address for the admin server
# Use 127.0.0.1 (default) to restrict to localhost only - access via SSH tunnel
# Use internal IP (e.g., 10.x.x.x) to allow access from VPN/private network
# Use 0.0.0.0 ONLY for local development - exposes to public internet!
ADMIN_BIND=127.0.0.1

# Path to OpenClaw config file
OPENCLAW_CONFIG=/root/.openclaw/openclaw.json

# Session secret for signing cookies (auto-generated if not set)
SESSION_SECRET=

# Management server URL for API proxy
MANAGEMENT_SERVER_URL=http://localhost:3000

# API token for management server (admin access)
MANAGEMENT_API_TOKEN=

# Agent server URL for container management
AGENT_SERVER_URL=http://localhost:3001
AGENT_SERVER_TOKEN=
